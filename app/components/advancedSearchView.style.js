import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
  },
  input: {
    borderColor: '#cccccc',
    borderRadius: 4,
    borderWidth: 1,
    color: '#555555',
    flex: 1,
    fontSize: 18,
    height: 36,
    marginRight: 5,
    padding: 10
  }
});
